class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        minval=float('inf')
        maxpro=0
        for i in prices:
            if i<minval:
                minval=i
            if i-minval>maxpro:
                maxpro=i-minval
        return maxpro
